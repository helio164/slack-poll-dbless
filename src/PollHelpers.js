const { KnownBlock } = require("@slack/types");
const { Button, ContextBlock, MrkdwnElement, PlainTextElement, SectionBlock, StaticSelect } = require("@slack/types");

module.exports = {
  buildButton(text, value) {
    // Rest of code...
  },
  buildTextElem(text) {
    // Rest of code...
  },
  buildSelectOption(text, value) {
    // Rest of code...
  },
  buildSectionBlock(text) {
    // Rest of code...
  },
  buildContextBlock(text) {
    // Rest of code...
  },
  appendIfMatching(optionArray, option, appendValue) {
    // Rest of code...
  }
}

