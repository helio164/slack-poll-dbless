const { KnownBlock } = require("@slack/types");
const { Modal } = require("@slack/types");
const { TextInput } = require("@slack/types");

module.exports = {
  PollModal: class {
    constructor() {
      // Rest of code...
    }
    getBlocks() {
      // Rest of code...
    }
  },
  ModalMap: {
    // Rest of code...
  }
}

